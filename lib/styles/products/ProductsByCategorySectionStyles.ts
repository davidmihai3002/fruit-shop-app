import { StyleSheet } from "react-native";

export const productsByCategorySectionStyles = StyleSheet.create({
    container: {
        gap: 30,
    },
    categoriesContainer: {
        flexDirection: "row",
        gap: 20,
        alignItems: "center",
    },
    scrollViewContent: {
        paddingVertical: 10,
    },
});

