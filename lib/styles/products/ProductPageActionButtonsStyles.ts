import { StyleSheet } from "react-native";

export const productPageActionButtonsStyles = StyleSheet.create({
    container: {
        flexDirection: "row",
        width: "100%",
        justifyContent: "space-between",
    },
    buttonWrapper: {
        width: 200,
    },
});

