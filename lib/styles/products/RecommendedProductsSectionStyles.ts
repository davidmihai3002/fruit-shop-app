import { StyleSheet } from "react-native";

export const recommendedProductsSectionStyles = StyleSheet.create({
    container: {
        gap: 24,
    },
    title: {
        fontSize: 24,
        fontWeight: "500",
    },
    scrollViewContent: {
        paddingVertical: 10,
    },
});

