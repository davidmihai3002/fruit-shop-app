import { StyleSheet } from "react-native";

export const addToFavoriteButtonStyles = StyleSheet.create({
    button: {
        padding: 15,
        borderRadius: 50,
        backgroundColor: "#FFF7F0",
    },
});

